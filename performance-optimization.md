# WinTab 卡顿风险优化

按已确认的方案继续修复，尽量保留正常合并时的无闪烁体验，不自动发布或打标签。

## 实施清单
- [x] 让鼠标设置读取不再等待文件保存；保存失败可检测，损坏设置可从备份恢复。
- [x] 限制重复登记和标签区域查询，避免繁忙时不断增加后台工作。
- [x] 为窗口操作设置期限；暂停、退出和资源管理器重启后，旧任务不能继续关闭或隐藏窗口。
- [x] 修复延迟双击、标签切换失败、磁盘根目录和更新超时等问题。
- [x] 修复安装器架构选择、运行库校验和不安全的强制结束流程。
- [x] 加入限量后台诊断，补齐回归测试，覆盖打包三个架构的安装器。

## 验收边界
- 不自动退出当前运行的 WinTab，不操作用户已有的资源管理器窗口。
- 自动测试使用模拟依赖及测试进程自己的隐藏窗口。
- 真实资源管理器的长时间对照和不同 Windows 版本的无闪烁体验，需要独立测试环境验证。
- 修复代码风险不等于已经证明整机卡顿完全解决。

## 补充保护
- 退出前等待设置保存，最多等待 1 秒；保存和窗口清理同时进行，不无限等待磁盘。
- 检查更新的 10 秒期限覆盖连接和内容读取，避免连接成功后一直等待响应正文。
- 诊断日志仍需显式设置 `WINTAB_DEBUG_LOG` 才启用；最多排队 256 条，文件最多 1 MiB，超限时保留较新的记录。
- 安装器只使用对应架构的正式 .NET 9 桌面运行库，并校验下载文件；安装与卸载均提示手动退出，不按进程名称强制结束程序，也不清空未知安装目录内容。

## 验证记录（2026-09-06）
- 已完整运行 `powershell -ExecutionPolicy Bypass -File .\build.ps1`，79 项自动测试通过。
- x64、x86、arm64 和合并安装器四种配置的运行库检查通过；这些检查在当前 x64 机器运行，不替代不同硬件上的实际安装验收。
- `dist/` 中的三个 `WinTab_v1.0.5_*_Setup.exe` 已覆盖更新，并核对版本、应用架构和文件校验值。
- 改动保留在当前工作区，未提交、推送、发布或打标签。
