<Window x:Class="WinTab.App.GroupHostWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        Title="WinTab Host" Width="1000" Height="700"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        SizeChanged="OnSizeChanged">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0"
                    ItemsSource="{Binding Tabs}"
                    SelectedItem="{Binding SelectedTab}"
                    Margin="0,0,0,8">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <Button Content="x" Width="16" Height="16" Padding="0"
                                Margin="6,0,0,0" DockPanel.Dock="Right"
                                Click="OnCloseTabClicked" Tag="{Binding}" Focusable="False" />
                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center" />
                    </DockPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>

        <wfi:WindowsFormsHost x:Name="HostSurface" Grid.Row="1">
            <wf:Panel x:Name="HostPanel" BackColor="Black" />
        </wfi:WindowsFormsHost>
    </Grid>
</Window>
