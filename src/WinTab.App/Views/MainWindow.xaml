<ui:FluentWindow x:Class="WinTab.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:pages="clr-namespace:WinTab.App.Views.Pages"
        Title="{DynamicResource App_Name}" Width="1180" Height="820" MinWidth="980" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="{DynamicResource App_Name}"
            Icon="pack://application:,,,/Assets/wintab.ico" />

        <Border Grid.Row="1"
                Margin="18,8,18,0"
                Style="{StaticResource AppPanelBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Width="52"
                        Height="52"
                        CornerRadius="14"
                        Background="{DynamicResource AppSurfaceStrongBrush}"
                        BorderBrush="{DynamicResource AppInfoBorderBrush}"
                        BorderThickness="1"
                        VerticalAlignment="Top">
                    <Image Source="pack://siteoforigin:,,,/Assets/logo.png"
                           Width="32"
                           Height="32"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                </Border>

                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                    <TextBlock Text="{DynamicResource App_Name}"
                               Style="{StaticResource PageSectionTitleStyle}" />
                    <TextBlock Text="{DynamicResource Shell_Tagline}"
                               Style="{StaticResource PageHeaderDescriptionStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <ui:NavigationView x:Name="RootNavigation"
                           Grid.Row="2"
                           PaneDisplayMode="Left"
                           IsPaneOpen="False"
                           IsBackButtonVisible="Collapsed"
                           IsPaneToggleVisible="True"
                           OpenPaneLength="228"
                           Margin="18,10,18,18"
                           Padding="10">



            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="{DynamicResource Nav_General}"
                                       TargetPageType="{x:Type pages:GeneralPage}"
                                       Icon="{ui:SymbolIcon Settings48}" />
                <ui:NavigationViewItem Content="{DynamicResource Nav_Behavior}"
                                       TargetPageType="{x:Type pages:BehaviorPage}"
                                       Icon="{ui:SymbolIcon CursorHover48}" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="{DynamicResource Nav_Uninstall}"
                                       TargetPageType="{x:Type pages:UninstallPage}"
                                       Icon="{ui:SymbolIcon Delete48}" />
                <ui:NavigationViewItem Content="{DynamicResource Nav_About}"
                                       TargetPageType="{x:Type pages:AboutPage}"
                                       >
                    <ui:NavigationViewItem.Icon>
                        <ui:ImageIcon Source="pack://siteoforigin:,,,/Assets/logo.png" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>

        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
