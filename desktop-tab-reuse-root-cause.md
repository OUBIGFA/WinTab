# 桌面标签复用根因修复

## 目标

让真实鼠标双击桌面文件夹时，稳定切换到已有标签，不闪烁、不新开残留窗口，并保留已有的断线恢复与预加载窗口修复。

## 任务

- [ ] 对照 v1.0.1、v1.0.5 与当前代码，确认活动标签识别和标签索引映射的真实差异。
- [ ] 增加行为测试，复现“目标是视觉第一个标签但内部误判成功”的情况，并先确认测试在旧逻辑上失败。
- [ ] 在统一的标签激活控制点修复根因，同时确保隐藏预加载窗口不参与计时、保护或复用目标选择。
- [ ] 运行完整测试，并在 WinTab 重新启动后通过用户真实鼠标双击核对日志与资源管理器实际标签。
- [ ] 运行完整构建，覆盖生成 x64、x86、ARM64 三个 1.0.5 安装器。

## 完成标准

- [ ] “新建文件夹 (3)”能从任意其他标签真实切换成功。
- [ ] 双击过程中无可见新窗口闪烁，也不留下第二个资源管理器窗口。
- [ ] 长时间运行与 Explorer 重启后仍能复用。
- [ ] 全部测试、差异检查和三个安装器构建通过。
