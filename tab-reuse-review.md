# 标签复用退化审查

对比基准：7b72abde3662cad8acc21f0caadc5c62535853e6。

## 本次工作

- [x] 对比标签登记、查找、切换与窗口恢复流程。
- [x] 补测首次读取失败、延迟登记和窗口恢复顺序，确认退化条件。
- [x] 修复已验证的问题，并检查关闭、停用和选中项保留。
- [x] 运行完整测试，覆盖生成三个架构的安装器。
- [x] 记录验证结果与未验证边界，清理本次临时文件。

不提交、不发布，不停止用户正在运行的 WinTab 或关闭已有资源管理器窗口。

## 修复结果

- 标签首次读取暂时失败时继续等待；启动时已有的标签也使用同一套登记流程。
- 切换前先恢复最小化窗口；目标标签已经消失时不再切换其他标签。
- 及时保存选中文件，读取暂时失败时保留上次结果，明确取消选择时清空旧记录。
- 复用标签时恢复请求的文件选择，不混入原先选中的文件；跳过不存在的文件，但全部文件不存在或无法读取文件视图时不再报告复用成功。

## 验证结果

- 2026-09-07：选中项相关的四项失败测试修复后通过，完整测试为 107/107 通过。
- 完整运行 build.ps1，三个架构均编译和打包成功；x64、x86、arm64 及自动选择架构的安装检查均通过。
- 三个 1.0.5 安装器已覆盖生成到 dist，版本号保持不变。
- 改动通过差异格式检查；18 个改动的 C# 文件均通过 UTF-8 和替换字符检查。

## 验证边界

窗口切换与标签复用测试使用独立测试窗口，没有替换正在运行的 WinTab，也没有操作用户已有的资源管理器窗口。真实桌面的首次标签、最小化后复用及选中文件场景，仍需安装新安装器后验证。

## 2026-09-07：恢复 v1.0.1 的复用可靠性

本轮以用户确认正常的 8cca3d780dc3a406517565c8dc672f1dd406caf2（v1.0.1）为体验基准，在 df55ea51997c66dc322edbe8d6237e21e189be74 上修复，不整体回退其它功能。

### 已验证的退化条件

- 已知文件夹仍被要求再次读取资源管理器地址；读取暂时失败时，原本可用的第一个标签被排除。
- 关闭时取消事件监听失败，会留下已经标记关闭的记录；之后重新打开的标签可能无法取得被旧记录占用的位置。
- 首次登记超过等待时间后，后续扫描只看到“已经登记”，不会补全尚未识别的标签。
- 单个窗口读取失败会打断整批登记；窗口仍开着时，已关闭标签的位置也可能被新标签重新使用。

### 修复内容

- 恢复 v1.0.1 的已知位置直接复用方式，正常命中不再额外读取地址；位置未知或不匹配时才查询当前地址。
- 等待标签就绪之前就开始监听导航和关闭，及时更新位置；查找时也能立即补全已就绪的第一个标签。
- 关闭清理不再被失效的事件连接阻断；登记前清除过期记录，分别核实窗口和标签的实际归属。
- 沿用现有的一秒检查补做未完成的登记；没有待处理记录时不反复完整扫描资源管理器。
- 单个窗口暂时不可读时保留其它窗口的登记结果，并安排重试；复用前再次核实对象，防止读取途中换成另一个标签。
- 登记等待恢复为旧版的一毫秒，保留现有的合并超时、窗口恢复和选中文件保护。

### 验证与安装包

- 新增 16 项测试，完整测试 123/123 通过；已确认失败的用例均经过修复前失败、修复后通过的验证。
- 包括 2,000 次已知位置重复复用、500 轮旧窗口或旧标签状态替换，以及 100 轮实际创建、关闭、重建隔离测试窗口。
- 完整运行 build.ps1，x64、x86、arm64 均编译并覆盖打包成功；三个架构及自动选择架构的安装检查全部通过。
- dist 中三个 WinTab_v1.0.5 安装器为本轮新生成的安装包，版本号未变；未提交、未推送、未打标签。

本轮没有停止或替换用户正在运行的 WinTab，也没有操作用户已有的资源管理器窗口。上述反复关开测试使用独立测试窗口，不等同于真实资源管理器连续使用数小时；真实长期稳定性和相对 v1.0.1 的实际操作速度仍需安装后验证。

## 2026-09-07：修复第三个标签开始明显变慢

### 原因与处理

- 查找新标签的重试曾离开负责资源管理器登记的线程，同一个标签因此产生两份不同的对象记录。
- 重复记录被当成尚未就绪的标签，每次登记都等待两秒，并在收尾时再等一次；后续打开文件夹和标签复用因此一起排队。
- 现在将新标签查找留在原有登记线程上，从源头避免重复记录。即使再次收到同一标签的登记，也保留原有效记录、清除重复项并立即结束，不占满等待时间。
- 保留原来的超时、窗口归属检查、关闭重开恢复及选中项保护；没有通过缩短保护时间掩盖卡顿。

### 验证记录

- 新增五项回归测试并更新一项已有保护用例。重复登记和线程归属问题均经过修复前失败、修复后通过的验证；完整测试为 128/128 通过。
- 三标签和五标签各重复登记 100 轮，登记数量始终与真实标签数量一致，同一文件夹的不同标签不会被误当成重复项。
- 真实资源管理器从两个标签逐步增加到五个标签，打开新文件夹、复用首个标签和最后一个标签均成功。最终复测未出现重复登记。
- 保持同一个 WinTab 进程，正常关闭资源管理器窗口后连续重开三轮；每轮打开第二、第三个文件夹并复用首个、第三个标签，全部成功，耗时约 1.0～1.4 秒。

最后一轮完整实测与修复前的同类操作对比（毫秒）：

| 场景 | 修复前 | 修复后 |
| --- | ---: | ---: |
| 打开第三个文件夹 | 3821 | 1399 |
| 三标签时复用首个标签 | 3327 | 1016 |
| 三标签时复用最后一个标签 | 3920 | 1293 |
| 打开第五个文件夹 | 3639 | 1320 |
| 五标签时复用首个标签 | 3336 | 1002 |
| 五标签时复用最后一个标签 | 4034 | 1346 |

计时包含启动资源管理器、完成合并或切换并关闭多余窗口的全过程，不只是内部查找耗时。以上是本机短时实测，不代表已经验证数小时连续使用，也没有重新运行 v1.0.1 安装包作同机对照。

- [x] 完整运行 build.ps1，覆盖生成 x64、x86、arm64 的 1.0.5 安装器并通过三个架构及自动选择架构的安装检查。
- [x] 核对改动、UTF-8 编码与安装器版本及校验值；本轮测试窗口均已关闭，临时文件已移入回收站。

## 2026-09-08：长期使用后复用、合并失效的恢复缺口

### 已确认的原因与版本范围

- 至少从 v1.0.1 起，程序就存在通知遗漏后无法自行恢复的缺口，并非到 v1.0.5 才首次出现。人为中断新窗口通知后，v1.0.1 与 v1.0.5 都无法继续合并，而本轮修复版可以恢复。
- 后续补查只处理已经登记但尚未就绪的标签；完全漏登的新窗口或新标签不会进入补查。因此，全关后重新出现的第一个标签也可能一直不可复用。
- 资源管理器的连接已经失效、但 explorer.exe 仍在运行时，原有流程不会主动重新建立连接，后续登记会持续失败。
- 这些证据确认了恢复缺口，不等于已经捕获用户连续使用数小时后自然触发失效的全过程，也不能据此排除其它触发条件。

### 本轮修复

- 沿用现有的一秒检查，核对实际标签与登记记录；只有发现漏登或尚未完成的登记时才安排补查。
- 正常登记的窗口、空桌面，以及没有实际标签的残留空窗口，不会持续触发完整扫描。
- 确认连接失效后，取消旧连接上的工作、恢复被隐藏的窗口，并通过现有初始化流程重新连接，无需重启资源管理器。
- 资源管理器暂时忙碌时仍保留原连接重试，不把临时拒绝响应当成断连。
- 不改变正常复用与合并的等待期限，不新增功能，不整体回退旧版本。

### 验证记录

| 场景 | 结果 |
| --- | --- |
| v1.0.1、v1.0.5 等待 30 秒后全关重开 | 各三轮通过；短时操作未自然复现长期失效 |
| 人为中断全部新窗口通知 | 两个旧版本均未能合并；修复版连续三轮合并与首标签复用通过 |
| 修复版释放旧连接、保持 explorer.exe 不退出 | 两轮通过，包括断连后的重新打开、合并与首标签复用 |
| 旧版本释放连接的真实桌面对照 | 测试开始时仍有窗口，未形成有效结果；不把准备失败记为产品失败 |
| 最后一项空窗口回归测试 | 修复前 137/138，修复后 138/138；完整打包时再次 138/138 |
| 安装器检查 | x64、x86、arm64 及自动选择架构检查全部通过 |

本轮共新增十项回归测试，覆盖漏登记、空窗后重开、正常状态不反复扫描、失效连接退出以及暂时忙碌的区别。原有的独立原生窗口一百轮关闭重建测试也通过；这些测试不冒充数小时真实使用验证。

### 交付与边界

- 已完整运行 build.ps1，于 2026-09-08 13:17 覆盖生成 dist 中 x64、x86、arm64 的三个 WinTab_v1.0.5 安装器；版本号未变，未提交、未推送、未打标签。
- 先前保存的五个文件夹已恢复，原有功能开关的值保持一致；收尾没有关闭用户窗口，也没有自动安装新包。
- 当前运行的仍是原先安装的 WinTab，验证本轮修复需要安装新生成的安装器。
- 尚未验证数小时自然使用后的表现，也没有完成本轮相对 v1.0.1 的完整速度基准对照。

## 2026-09-08：桌面双击只唤起窗口、未切回对应标签

### 现场与根因

- 用户报告直接双击桌面的“38 Camp Mograph”只闪烁，以及“新建文件夹 (2)”出现第二个窗口。最初运行的安装版确实来自 13:17 的上一轮打包，不能归因于没有安装更新。
- 在不重启资源管理器的情况下读取程序现场：两个目标的路径、标签和窗口记录均已登记，后台登记任务空闲。原进程未启用 WINTAB_DEBUG_LOG，没有历史操作日志可供追溯。
- 只重启 WinTab 开启诊断后，直接启动路径能成功，但用户的桌面双击没有新窗口登记日志。18:09、18:15 与 18:19 的桌面事件证实：Explorer 已找到容纳该文件夹的旧窗口，只唤起该窗口，却没有把对应标签切到前台，也没有产生新窗口登记或导航通知。
- 因此，上轮针对通知遗漏和失效连接的修复没有覆盖这条路径。不能用启动路径成功代替桌面双击成功，也不能把“没有合并日志”理解为用户没有操作。

### 本轮处理

- 连接桌面的默认打开通知，读取本次实际打开的文件夹；使用现有的路径查找与标签切换流程，补齐没有新窗口通知的复用请求。
- 不拦截系统默认打开，不改注册表，不针对报告中的名称硬编码。不匹配已有标签时，仍由原有打开和合并流程处理。
- 仅处理单个真实文件夹；文件、快捷方式、虚拟项目、多选，以及关闭合并或复用时保持原行为。快速重复请求合并处理，不新增周期性扫描。
- 桌面事件与原有资源管理器连接一起建立、取消和释放；停止或断连后不再执行旧请求。每次读取后释放临时桌面对象，保留现有的窗口归属与超时保护。

### 验证与交付

- 修复前，18:19 的同一次观察捕获四次“38 Camp Mograph”桌面默认打开，旧窗口始终显示另一标签，实际断言失败。
- 修复后，18:30 同场景切回首标签；18:31 的多次桌面操作日志也确认复用成功，18:34 的“新建文件夹 (2)”回到已有标签。
- 增加十四项行为测试，覆盖正确路径、中文与括号、非文件夹输入、功能开关、断连和事件退出；完整构建中 152/152 项测试通过。
- 18:49 的交付复测出现过“切换已成功、观察结束时已是另一标签”，另一次诊断枚举遇到已消失对象；这两次不计为通过。修正临时观察工具后，18:52、18:53 使用最终 x64 发布程序连续复测两个桌面图标，实际默认打开通知、窗口标题及活动标签均匹配，观察结束时均通过，未留下重复文件夹窗口。
- 完整运行 build.ps1，18:47 覆盖生成 x64、x86、arm64 三个 WinTab_v1.0.5 安装器；三种架构及自动选择架构的安装检查全部通过。版本号未变，未提交、未推送、未打标签。
- x64 安装器 SHA-256：02AFEC735E1FEE299534BD70FEA3A632C6A51A4DC0B5F52E1898AF681FBB3DBE。
- 最终保留 publish/net9.0-windows/x64 中的修复版运行，不自动覆盖安装目录。开机启动仍指向原安装目录，需要用户运行 dist 中的新安装器，才能在下次开机继续使用本轮修复。
- 用户设置文件未更改；没有关闭用户原有资源管理器窗口。没有宣称完成数小时自然使用验证或与 v1.0.1 的完整速度对照。

## 2026-09-09：恢复 v1.0.1 的桌面文件夹打开机制

### 原因

- 长时间使用后，桌面双击会先被 WinTab 的默认打开拦截器接管；当后台复用准备过期、连接暂时不可用或切换失败时，系统原生打开会被延后，随后又由窗口登记流程合并，形成“新开窗口后闪烁并关闭”的体验。
- 这条拦截路径不是 v1.0.1 的行为，继续叠加修补会让桌面打开和资源管理器自身的窗口登记相互竞争。

### 本轮处理

- 移除桌面默认打开拦截、桌面打开队列和预读桌面选中项逻辑。
- 恢复由 Explorer 原生打开，再通过原有的 WinEvent 与 `ShellWindows.WindowRegistered` 登记流程隐藏新窗口并完成标签合并。
- 保留此前已验证的连接断开恢复、漏登记补查、窗口与标签句柄校验、关闭清理、选中文件保护及合并超时恢复等修复；本轮只回退引发闪烁的桌面接管机制，不整体回退到旧版本代码。
- 增加回归检查，确保生产代码不再挂接桌面默认打开拦截器；普通标签复用测试继续保留。

### 验证与交付

- Release 编译与完整测试：152/152 通过。
- 已完整运行 `build.ps1`，x64、x86、arm64 安装器均重新覆盖生成；安装器运行环境检查（包括自动选择架构）全部通过。
- 版本号保持 1.0.5；没有提交、推送或打 Tag。五个废弃的桌面拦截文件已移入系统回收站。
- 真实长时间使用仍需安装 `dist` 中的新安装器后观察；测试证明了机制回退和现有回归行为，但不能替代数小时桌面操作验证。
