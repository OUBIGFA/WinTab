# WinTab 版本对比审查报告

- 审查日期：2026-09-06
- 基准版本：`3eefb2e1d0c6fc8eba34620faa1dc4331a9af7f4`（Release v1.0.5）
- 目标版本：`763f15e2b6e4ba5043760ef42f5d64f45b3f2caa`
- 审查类型：版本差异审查（PR Review）

## 一、结论

**算有实质优化，但不能认定为“全面更优”或“已经解决卡顿”。建议保留优化方向，先修复本报告确认的问题，再进行真实使用场景的性能对照。**

新版确实减少了部分磁盘等待、重复后台任务和无效的延迟操作，设置保存、诊断日志、更新检查和安装保护也有实际改进。但补充验证发现 **3 项问题**，另有 **1 项尚需真实资源管理器验收的行为风险**。

这些问题不是“所有用户必然遇到”，而是在下面列出的特定条件下出现。不能因为自动测试全部通过，就把这次改动直接视为已经验证的稳定升级。

## 二、审查范围

两版之间只有一次提交，涉及 **41 个文件，新增 2,580 行、删除 960 行**。两版项目版本号均为 1.0.5。

本次核对了变更清单及生产代码差异，对窗口合并与恢复、后台查询、设置保存、更新检查和安装保护作重点审查，并抽查测试覆盖情况。由于改动较大，这是高风险路径重点审查，不是所有路径的穷举审计。

验证使用两个独立的提交副本，没有切换工作分支，没有修改产品代码，没有操作用户已有的资源管理器窗口。补充窗口实验只使用测试进程自己的隐藏或离屏窗口。

## 三、确认的问题

### 1. 较高风险：部分坏设置会阻止启动，即使有备份也无法恢复

**触发条件**：设置文件的格式合法，但某些值不合法。例如，窗口宽度被写成负数。

**发现与证据**：在临时设置文件中写入 `{"FormSize":{"Width":-1,"Height":720}}`，同时提供内容正常的备份。新版读取设置时直接报错，没有使用备份。

**为什么是退步**：旧版对这类读取失败会回到默认设置；新版只处理部分错误，窗口尺寸引发的错误不在处理范围内。程序启动时就会读取这些设置，因此它不只是“某个设置没保存”，还可能让程序无法启动。

**建议**：读取后先校验窗口尺寸等配置值，把非法值纳入明确的备份恢复流程；不要简单改成不区分原因地忽略所有错误。补充“格式正确、内容非法”和“备份也损坏”的测试。

定位：`WinTab/Managers/SettingsStore.cs:122`、`WinTab/Managers/SettingsStore.cs:143`、`WinTab/UI/Views/MainWindow.xaml.cs:37`。

### 2. 较高风险：异常退出后，之前被隐藏的窗口失去了原有的自救能力

**触发条件**：合并过程中窗口已被变透明，但 WinTab 在恢复前崩溃或被强制结束，内存中的恢复记录随之丢失。

**发现与证据**：对同一类自建测试窗口，先隐藏，再模拟恢复记录丢失，然后调用各版本的恢复方法：

| 对照版本 | 恢复方法的结果 | 窗口是否仍然透明 |
| --- | --- | --- |
| 旧版 | 成功 | 否 |
| 新版 | 未恢复 | 是 |

**为什么是退步**：旧版能识别并修复没有记录的透明资源管理器窗口；新版仅处理本次运行中记录过的窗口。正常退出的恢复逻辑仍然存在，但程序重新启动后，已经丢失的记录不会自动回来。

这里验证的是“记录丢失后的恢复方法”，不是在用户的资源管理器上执行崩溃测试。

**建议**：保留可以验证归属的跨启动恢复记录，使下次启动能恢复确实由 WinTab 隐藏的窗口。不要为了恢复能力，重新无差别修改其它软件设置的透明度或窗口位置。

定位：`WinTab/Helpers/ExplorerWindowVisibility.cs:60`、`WinTab/Helpers/ExplorerWindowVisibility.cs:71`、`WinTab/Hooks/ExplorerWatcher.cs:1893`。

### 3. 中风险：一个慢窗口会挡住其它窗口的标签识别

**触发条件**：某个窗口的标签区域查询迟迟不返回，其它窗口又需要更新标签区域记录。

**发现与证据**：补充实验让第一个窗口的查询等待，第二个窗口的查询可以立即完成。但等待 400 毫秒后，第二个窗口仍没有结果；只有放行第一个查询后，第二个窗口才得到结果。

**为什么是退步**：旧版不同窗口的后台查询可以分别推进；新版所有窗口共用一个查询线程，而且单次查询没有隔离或超时退出措施。限制后台任务数量是好事，但现在一个慢查询会影响所有排在后面的窗口。

**影响范围**：主要影响标签区域识别及依赖它的双击关闭功能；不能据此声称整个鼠标或整台电脑都会卡死。已有且仍有效的缓存也不一定立刻受影响。

**建议**：保留总量限制，同时隔离不同窗口的慢查询，保证一个窗口无响应时其它窗口仍能更新。补充“一慢一快两个窗口”的测试，而不是回退到无限创建后台任务。

定位：`WinTab/Helpers/BackgroundRefreshCache.cs:75`、`WinTab/Helpers/BackgroundRefreshCache.cs:86`、`WinTab/Hooks/TabStripHitTester.cs:64`。

## 四、尚需真实场景确认的风险

### 文件选中状态可能丢失或过期

旧版在转移窗口内容时读取选中项；新版使用定时保存的选中项记录，采集间隔为 500 毫秒。新登记的窗口一开始没有这份记录。

因此，如果窗口在首次采集前就进入合并流程，或者用户刚改变选择就关闭窗口，转移或重新打开时使用的记录可能为空或已经过期。需要重点验证“从其它程序打开文件所在位置并选中文件”，以及“快速关闭后重新打开”的场景。

**这项有代码路径依据，但本次没有在真实资源管理器中复现，不与前面 3 项隔离实验同等定性。**

建议保留避免在关闭回调里执行慢读取的方向，但在真正转移选中状态前保证记录足够新，并补充对应的完整场景测试。

定位：`WinTab/Hooks/ExplorerWatcher.cs:670`、`WinTab/Hooks/ExplorerWatcher.cs:916`、`WinTab/Hooks/ExplorerWatcher.Lifecycle.cs:41`、`WinTab/Hooks/ExplorerWatcher.Lifecycle.cs:102`、`WinTab/Hooks/ExplorerWatcher.Lifecycle.cs:180`。

## 五、哪些改动确实值得保留

| 方向 | 实际改进 | 判断边界 |
| --- | --- | --- |
| 设置读写 | 鼠标相关的设置读取不再与磁盘写入共用等待；保存移到后台，并增加安全替换与备份 | 已有阻塞写入等测试通过，但第 1 项坏值恢复问题需要补上 |
| 重复窗口登记 | 大量重复请求只保留当前处理和最新一轮待处理工作 | 自动测试验证 1,000 次请求不会变成 1,000 次并发扫描 |
| 合并期间的重复隐藏检查 | 从反复扫描资源管理器窗口，改为只处理已记录的合并源，减少无关窗口遍历 | 确实减少了不必要的工作，但实际 CPU 收益仍需测量 |
| 过期窗口操作与恢复 | 增加有效期、窗口身份和暂停/重启后的有效性检查；恢复时保留原有透明度 | 有实质防护，但不能中断所有已经开始的系统调用，也需补回跨启动恢复 |
| 标签区域记录 | 限制队列规模，避免已失效的旧查询结果重新写回 | 有资源控制收益，但存在第 3 项跨窗口相互等待问题 |
| 诊断日志 | 后台写入，限制排队量和文件大小 | 旧版默认也不开诊断日志；主要收益发生在主动启用日志时 |
| 更新检查 | 超时覆盖响应正文读取，区分用户取消；不再随意返回不匹配架构的安装包 | 对应自动测试通过，不能等同于所有实际网络环境已验收 |
| 安装保护 | 增加下载校验和架构检查，识别需要重启的安装结果；不再按进程名强制结束程序，也不清空未知安装目录内容 | 四种配置自检通过，未执行真实运行库下载或安装 |

**需要纠正一个容易产生的印象：旧版已经把标签区域查询放在后台，并已有缓存。** 本次不是首次把鼠标查询从同步改成后台，主要变化是集中管理、限制数量和校验旧结果；应按这些真实变化评价收益。

## 六、不能据此宣布“卡顿已经解决”

新版的等待期限并不能强制中断所有已经卡住的系统调用。

补充实验为一次检查设置 40 毫秒期限，但让检查本身先同步等待 250 毫秒：新版仍耗时 **264 毫秒**；旧版对照为 **530 毫秒**。这说明新版确实减少了到期后的多余执行，但没有让这类同步等待严格受期限控制。

这只是验证等待机制的边界，**不是整机性能基准，也不能据此计算实际使用的提速比例**。

定位：`WinTab/Helpers/Helper.cs:45`。

本次没有测量长期 CPU 占用、常驻内存、真实文件夹合并耗时或不同 Windows 版本的表现，也没有执行会操作用户现有窗口的压力测试。因此不能给出“明显快了多少”或“整机卡顿彻底消失”的结论。

## 七、验证记录

| 验证项目 | 本次结果 |
| --- | --- |
| 旧版独立编译 | 成功 |
| 旧版现有自动测试 | 48 / 48 通过 |
| 新版独立编译 | 成功 |
| 新版现有自动测试 | 79 / 79 通过 |
| 安装器运行库规则自检 | x64、x86、arm64、combined 四种配置均通过 |
| 坏设置且存在有效备份 | 新版加载失败，报错类型为 `ArgumentException` |
| 丢失隐藏记录后的恢复 | 旧版恢复，新版仍然透明 |
| 两个窗口一慢一快的查询 | 新版快速查询必须等待慢查询让路 |
| 自建窗口关闭命令 | 返回成功，窗口确实消失；未将此项列为问题 |
| 同步检查的等待期限 | 新版 264 毫秒、旧版 530 毫秒，均超过设定的 40 毫秒 |

两版使用相同的本机编译工具链；环境安装的是 `.NET SDK 11.0.100-preview.3.26207.106`，项目目标仍为 `.NET 9 Windows`。

安装器自检只验证规则和配置，不代表三个硬件架构都实际安装通过，也没有验证远端下载文件的实际内容。自检程序有意在安装开始前退出，因此 `PASS` 配合退出码 1 是该测试约定的成功结果。

新版比旧版多通过 31 项自动测试是积极变化，但目前这些测试没有覆盖本报告发现的全部边界。测试数量增加，不等于真实窗口合并的完整过程已被覆盖。

## 八、采用建议

1. **先修设置内容校验与备份恢复**，避免坏数据导致程序无法启动。
2. **补回安全的跨启动窗口恢复**，防止异常退出后留下透明窗口。
3. **隔离慢窗口查询**，保留任务数量限制，但不要让所有窗口互相拖住。
4. **验收选中项转移和快速重开场景**，确认没有牺牲原有行为。
5. 最后在同一环境下对照两版，记录合并耗时、CPU、内存和长时间使用表现，再判断是否达到整体优化目标。

**最终判断：这是一次包含实质收益的性能与安全性改造，但目前仍有回归问题；更准确的评价是“方向正确，需要补齐”，而不是“新版必然更好”。**

本次仅交付审查报告，不修复产品代码，不提交、不推送、不打标签，也不覆盖正式安装器。临时副本、验证程序和日志在交付前按回收站规则清理。

## 九、后续修复与验证（2026-09-06）

以上保留原始审查结论。本节记录用户确认方案后的修复，不代表已经发布新版本。

- **设置恢复**：非法窗口尺寸进入正常的备份恢复流程；备份也无效时使用默认值，并保留错误原因。
- **窗口恢复**：隐藏前把原有透明度、颜色键及归属标记保存在对应窗口上。记录可跨 WinTab 进程退出保留，成功恢复后清除；原窗口销毁时也由系统清除，不依靠容易复用的窗口编号单独判断归属。没有本次新增标记的旧透明窗口不自动修改。
- **慢查询隔离**：使用两个固定查询线程，保留请求合并、队列上限和过期结果保护；一个慢查询不再阻挡其它窗口。两个查询都卡住时仍会占满处理能力，本次没有强行终止系统查询。
- **选中项保留**：合并前补采集一次；收到关闭通知时，若原窗口仍可访问，也补采集一次。读取失败保留最近有效记录，明确清空选择时清除旧记录，窗口关闭或位置改变后的迟到结果不再写入。鼠标钩子中未加入同步选中项查询。

验证结果：

- 自动测试由 79 项增加到 **97 项，97/97 通过**。新增测试覆盖设置异常、慢查询隔离、跨进程窗口恢复及选中项记录。
- 窗口恢复测试使用自建隐藏窗口，并让负责隐藏的独立测试进程退出，验证另一进程能恢复原状态；未操作用户已有的资源管理器窗口。
- 完整运行 `powershell -ExecutionPolicy Bypass -File .\build.ps1`，退出码为 0。安装器运行库规则自检的 x64、x86、arm64、combined 四种配置均通过。
- 已覆盖 `dist/` 下版本号仍为 **1.0.5** 的 x64、x86、arm64 三个安装器；没有提交、推送或打标签。

仍需安装后重点确认：从其它程序打开文件所在位置并选中文件、刚改变选择就关闭并快速重新打开。选中项测试验证的是记录更新规则，并未替代真实资源管理器的完整交互验收；本次也未在三种硬件架构上实际安装，未测量整机性能或长时间使用表现。
