# 最新提交审查报告

审查日期：2026-09-10。审查模式：PR Review。

审查对象：8963e2c68d195d63020011778c1021c9dec3b653，相对父提交 7368336。
范围：26 个变更文件，重点深入检查桌面标签复用、隐藏预加载窗口、关闭与恢复流程，以及新增测试；设置界面检查布局、资源引用和交互代码。本次是审查，没有修改产品代码。

结论：发现两项需要修复的问题，建议先处理延迟关闭，再处理桌面目标识别。现有测试为 176/177 通过，不能据此认定本次改动已经通过完整验证。

## 1. 高优先级：超时恢复不能撤销已排队的关闭请求

位置：WinTab/Hooks/ExplorerWatcher.cs:870；关联关闭结果判断与恢复逻辑位于同文件 834、844、849 行。

**现象与触发条件：** 资源管理器暂时忙碌、无法及时处理关闭请求时，程序在等待结束后判断关闭失败并进入恢复流程，但排队的关闭请求仍会继续生效。窗口可能在已经被当作恢复对象后关闭。

**原因：** 本次把关闭改成 PostMessage。它成功仅代表请求已进入系统队列。后续移除关闭记录、取消操作或恢复窗口，都不会撤销已经投递的 WM_CLOSE；投递前对窗口归属和标签数量的检查，也不能约束稍后处理消息时的状态。

**验证证据：** 使用独立线程上的原生测试窗口，让窗口先忙碌约 2.5 秒，再调用实际的关闭流程。两次诊断均返回 closed=False，并记录“未关闭，已恢复”，但返回时窗口已经不存在。最近一次耗时约 2303 毫秒。对照原先的同步限时发送，在同类忙碌条件下约 212 毫秒后返回未确认，随后窗口仍然存在。

**影响：** 关闭结果与实际窗口状态不一致，超时恢复的保护失效；若窗口状态在等待期间改变，还存在关闭已交还用户使用的窗口的风险。验证没有证明发生文件数据丢失。

**建议：** 统一关闭确认与恢复的处理，避免在决定放弃关闭后仍留下会生效的请求。若关闭请求仍处于未决状态，应继续跟踪最终结果，不能报告已经安全恢复。补充窗口先忙碌、随后超时，以及停用功能时仍有关闭请求的行为测试。

## 2. 中优先级：稍后读取的桌面选中项可能不是刚打开的文件夹

位置：WinTab/Hooks/ExplorerDesktopOpenObserver.cs:40、60；实际切换入口为 WinTab/Hooks/ExplorerWatcher.DesktopOpen.cs:79。

**现象与触发条件：** 双击桌面文件夹 A 后，在后台通知执行前单击选中文件夹 B，程序会把 B 当成本次打开的目标。若 B 已有标签，可能切到 B；A 的标签复用请求则被遗漏。

**原因：** 打开通知只安排稍后读取 SelectedItems，没有保存或核实这次打开与目标的对应关系。读取的是执行时的桌面选择，而非打开动作发生时的目标。现有测试中的选择始终不变，因此没有覆盖这个时序。

**验证证据：** 对实际观察者执行“选中 A → 触发打开 → 选中 B → 执行排队任务”，两次均得到 allowed=True、reported=C:\Review\Folder-B。原生打开已经放行，但补充复用收到错误路径。

**影响：** 快速连续操作或后台任务排队时，可能切错标签，或者再次出现“窗口被唤起但没有切到目标标签”的问题。

**建议：** 保持系统默认打开立即放行，同时核实复用目标确实属于这次打开；选择已变化、无法确认目标时应放弃这次补充切换。不要直接把后台执行时的选中项视为已打开项，也不要通过拦截或延迟系统打开规避问题。

## 验证结果与边界

| 检查 | 结果 |
| --- | --- |
| 独立副本编译 | 已有编译日志显示成功；没有重复完整编译 |
| 副本一致性 | 本次变更的 15 个产品文件逐一核对，与该提交内容一致 |
| 完整测试 | 已有完整日志为 176/177 通过 |
| 唯一失败项 | a slow tab-switch acknowledgement is not a failed switch |
| 慢速切换复核 | 两轮诊断中，各自直接重跑原单测两次均通过；随后同条件诊断均未切到目标，约 2.6 秒后返回失败；单独发送切换命令成功 |
| 桌面目标变化 | 已复现错误目标传递 |
| 忙碌窗口延迟关闭 | 已复现失败结果与实际关闭状态不一致，并完成旧发送方式对照 |
| 调度失败边界 | 已验证调度器关闭时异常被包装成 TaskSchedulerException，观察者现有捕获条件未覆盖；尚未验证真实桌面上是否触发或影响默认打开，因此未列为已确认产品缺陷 |
| 设置界面 | 完成代码与样式检查；没有进行真实窗口在不同显示缩放下的视觉验收 |

慢速切换结果说明验证仍不稳定，但目前没有足够证据把它单独归因于本次产品改动，不将其包装成已确认的新回归。

窗口验证使用隔离桌面和测试窗口，没有关闭用户已有资源管理器窗口，也没有替换正在运行的 WinTab。这些结果不等同于真实桌面连续使用数小时的验证。

本次交付仅包含审查报告；产品代码、安装器和版本号未更改，未提交、推送或发布。